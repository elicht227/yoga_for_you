<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Teaching Events

    <a data-toggle="collapse" href="#teacher_teaching_events_new_form">
      Add event
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @teacher.teaching_events.each do |event| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to event, event %>

        <%= link_to event, method: :delete, data: { confirm: "Do you really want to delete this event" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="teacher_teaching_events_new_form">
        <%= render partial: "teachers/teacher_teaching_events_new_form" %>
      </div>
    </div>

  </ul>
</div>
