<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Events

    <a data-toggle="collapse" href="#category_events_new_form">
      Add event
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @category.events.each do |event| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <!-- Show link start -->
        <%= link_to event, event %>
        <!-- Show link end -->

        <!-- Delete link start -->
        <%= link_to event, method: :delete, data: { confirm: "Do you really want to delete this event" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>
        <!-- Delete link end -->

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="category_events_new_form">
        <!-- New associationcollection form start -->
        <%= render partial: "categories/category_events_new_form" %>
        <!-- New associationcollection form end -->
      </div>
    </div>

  </ul>
</div>
