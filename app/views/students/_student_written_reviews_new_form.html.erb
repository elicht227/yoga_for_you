<%= form_with(model: @review, local: true) do |f| %>

  <div class="form-group">
    <%= f.label :star_rating %>

    <%= f.text_field :star_rating, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :review_text %>
    <%= f.text_area :review_text, rows: "3", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :teacher_id %>

    <%= f.collection_select :teacher_id, Teacher.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <%= f.hidden_field :student_id, value: @student.id %>

  <%= f.submit "Create review", class: "btn btn-block btn-outline-secondary" %>
<% end %>
