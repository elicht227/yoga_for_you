<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render object: @student, partial: "card", as: :student %>
  </div>
</div>

  <!-- Show Page Customization starts -->
<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "students/student_written_reviews" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "students/student_attendee_ids" %>
  </div>
</div>

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <%= render partial: "students/student_events_attending" %>
  </div>
</div>
  <!-- Show Page Customization end -->
