<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Attendee Ids

    <a data-toggle="collapse" href="#student_attendee_ids_new_form">
      Add attendee
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @student.attendee_ids.each do |attendee| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <%= link_to attendee, attendee %>

        <%= link_to attendee, method: :delete, data: { confirm: "Do you really want to delete this attendee" } do %>
          <i class="fas fa-trash-alt"></i>
        <% end %>

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="student_attendee_ids_new_form">
        <%= render partial: "students/student_attendee_ids_new_form" %>
      </div>
    </div>

  </ul>
</div>
